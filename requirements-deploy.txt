# Lätta beroenden för CPU-deploy (Render) – ingen torch, ingen matplotlib.
# Backenden kör syntetiska warp-brädor + facit-segmentering (ingen modell).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
numpy>=1.24
scipy>=1.10
pillow>=10.0
